#!/usr/bin/env python

import heapq
import sys


def main():
    q = int(sys.stdin.readline())
    a = []
    for i in range(q):
        lst = [int(elem) for elem in sys.stdin.readline().split()]
        c = lst[0]
        x = lst[1]
        num_to_count = {}
        nums = []
        if c == 1:

        elif c == 2:
            heapq.
            pass
        elif c == 3:
            pass
        else:
            raise RuntimeError(f"Unknown C {c}")


if __name__ == "__main__":
    main()
